# 轮次记录：VideOCR 预检与验收准备完成

- 时间：2026-07-22
- 用户目标：使用用户提供的 VideOCR CLI GPU v1.5.1 完成 v0.7.1 验收工具预检，然后继续冻结和真实媒体验收。

## 已知事实与证据

- VideOCR CLI 目录包含 GPU CUDA 12.9 PaddleOCR 组件和非空 `videocr-cli.exe`。
- 可执行文件 SHA256 已记录到私有预检指纹；帮助命令成功并包含所有冻结参数。
- 可执行文件带 `CN=timminator` 自签名证书，但证书链不受 Windows 信任；该信息只作辅助来源证据。
- 用户未提供原始官方 `.7z` 归档，因此私有指纹如实记录 `archive_verified=false`。
- 六个候选视频均已存在，本轮只做文件清单检查，尚未读取媒体内容。

## 决策摘要

- 允许用户显式提供仓库外的 VideOCR 可执行文件，但 OCR SRT、日志、指纹和运行记录仍必须位于 Git 忽略的私有验收目录。
- 只允许本地 PaddleOCR，云 OCR 继续硬拒绝。
- 缺少原始归档哈希不得被隐藏；后续公开报告只写工具版本和不可逆指纹，不写本地路径。

## 执行操作

- 检查 VideOCR 目录结构、可执行文件大小、SHA256、Authenticode 签名和实际 `-h` 输出。
- 调整验收运行器，允许外部可执行文件，但继续强制私有输出边界和本地 OCR 限制。
- 生成 VideOCR 私有预检 JSON，未记录可执行文件的绝对路径。
- 重新以 `local_files_only=True`、CUDA/float16 实际加载并释放 `small` 和 `large-v3`。

## 验证结果

- VideOCR 帮助契约预检通过，适配器定向测试 4 项通过。
- 完整 `pytest -q` 通过。
- 导入检查、两个 self-test、Web smoke、两个 HTTP 200 检查、Electron 三个 JavaScript 语法检查和 `git diff --check` 均通过。
- 没有 VideOCR 下载、解压或 OCR 运行进程留在后台。

## 未解决问题与下一步

- 创建并推送 `v0.7.1: prepare real-media acceptance` 提交。
- 在提交后冻结 evaluated SHA、模型、VideOCR 和环境指纹。
- 执行六视频低成本预筛，生成三个匿名候选片段说明，等待用户确认。
