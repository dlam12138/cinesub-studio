# 轮次记录：冻结指纹补齐模型预检

- 时间：2026-07-22
- 用户目标：完成 v0.7.1 preparation 提交、推送和环境冻结，随后开始六视频预筛。

## 已知事实与证据

- `v0.7.1: prepare real-media acceptance` 已创建并推送到验收分支。
- 开始冻结前复核发现，环境指纹包含 OCR 预检，但未嵌入已生成的模型 revision/hash 预检文件。
- 尚未开始任何真实媒体处理，因此允许在冻结前修正验收 runner。

## 决策摘要

- 不使用分散文件代替既定指纹契约；将 OCR 和模型预检都嵌入同一环境冻结 JSON。
- 不改写已推送提交，使用独立小型 preparation 修正提交保留审计历史。

## 执行操作

- 为 `fingerprint` 子命令新增必需的 `--model-preflight`。
- 环境指纹新增 `models` 结构，直接嵌入私有模型预检结果。
- 新增单元测试，验证 OCR 与模型预检均进入冻结指纹。

## 验证结果

- 验收 runner 定向测试 5 项通过。
- `git diff --check` 通过。

## 未解决问题与下一步

- 创建并推送指纹补齐提交。
- 用新提交 SHA 生成正式私有环境指纹，确认工作区干净后开始预筛。
