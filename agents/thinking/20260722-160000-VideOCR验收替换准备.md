# 轮次记录：VideOCR 验收替换准备

- 时间：2026-07-22
- 用户目标：使用 GitHub 上的 VideOCR CLI 代替法语 Windows OCR 作为 v0.7.1 真实媒体验收的硬字幕弱证据，不集成进 CineSub 产品链路。

## 已知事实与证据

- 官方 GitHub Release v1.5.1 提供 Windows CPU 和 CUDA 12.9 CLI 压缩包，并公布 SHA256。
- 当前 GPU 为 RTX 4060 Laptop，适用 CUDA 12.9 构建。
- v1.5.1 上游源码已固定到 `ab4599dd8d55978ee2b29169c9e1b40dd0bae316`，CLI 参数实现已与验收适配器核对。
- GitHub Release 大文件在当前网络下速度异常：两个官方下载通道都未在时限内完成。

## 决策摘要

- VideOCR 只允许在 Git 忽略的私有验收目录中运行，只允许本地 `paddleocr`，明确禁止 `google_lens`。
- OCR 结果只是弱证据，不自动决定 retry window 的接受或拒绝。
- Windows OCR 降为可选交叉证据，不再是环境冻结前置条件。
- 未完成官方归档哈希和可执行文件预检前，不创建 preparation 提交，不开始真实媒体预筛。

## 执行操作

- 查证官方 Release 资产名称、大小、SHA256、许可证和 CLI 参数。
- 新增 VideOCR 归档与可执行文件预检、私有路径边界、本地 OCR 命令构造、运行记录和 SRT 哈希记录。
- 更新治理文档和匿名验收方法文档，并新增本地引擎与禁止云 OCR 测试。
- 尝试 `curl` 和 GitHub CLI 官方认证下载，超时后终止所有遗留进程，保留私有断点文件。

## 验证结果

- 完整 `pytest -q` 通过。
- 导入检查、两个 self-test、Web smoke、`/` 与 `/api/runtime/diagnostics` 200、Electron 三个 JavaScript 语法检查均通过。
- `git diff --check` 通过；私有 VideOCR 资产命中 Git 忽略规则。
- 定向 VideOCR 适配器与 OCR 工具测试共 15 项通过。

## 未解决问题与下一步

- 需完成官方 VideOCR CLI 归档下载、SHA256 校验、解压和 `-h` 真实预检。
- 预检通过后重跑必要回归，创建并推送 `v0.7.1: prepare real-media acceptance`。
- 随后冻结环境并从六个视频中生成匿名候选片段，等待用户确认三个样本。
