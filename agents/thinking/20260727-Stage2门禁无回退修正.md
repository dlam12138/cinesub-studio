# Stage 2 No-Regression Gate Correction

## User Goal

Correct the Stage 2 conclusion in PR 17 from a repository-wide zero-Ruff gate
to a scoped no-regression gate, without changing production code or rerunning
the completed real-media scenarios.

## Facts And Evidence

- S1, S2, S3, and S7 remain Pass; their recorded results were not changed.
- Targeted tests previously passed 55/55 and full pytest passed 526/526.
- Both existing PR 17 Windows CI runs passed.
- The merge-base is `1b1ae9b`.
- Full Ruff reports 182 findings in both the merge-base and current branch.
- Ruff over the seven changed Python files reports 30 findings in both the
  merge-base and current branch.
- Repository-wide and changed-file finding deltas are both 0.

## Decision

- Treat existing Ruff findings as separately tracked historical debt.
- Use scoped no-regression as the Stage 2 lint gate.
- Set Stage 2 to Pass, release blocker false, Stage 3 allowed, and Release
  Prep not allowed.
- Do not claim that repository-wide Ruff or direct whole-file Ruff is clean.

## Operations

- Updated the rerun Markdown, JSON, CSV, and prior thinking evidence.
- Preserved the original failed Stage 2 report and all real-media outcomes.
- No production code, test logic, configuration, media, output, tag, release,
  or Stage 3 implementation was changed.

## Validation And Next

- Re-run full pytest, report parsing/consistency, privacy scanning,
  `git diff --check`, and Ruff no-regression comparison.
- Update PR 17, push one evidence-only commit, and wait for both Windows CI
  checks.
- After merge, return to the ASR and translation quality campaign.
