# 轮次记录：Attempt 3 证据不足降级

- 时间：2026-07-22
- 用户目标：完成三个匿名样本的 v0.7.1 真实媒体矩阵，并按冻结规则决定 `quality` 是否保留自动 apply。

## 已知事实与证据

- Attempt 3 使用 remediation SHA `c07496e706c8dd7596eeb043532b45c146294eab`，三个样本、模型 revision 和媒体哈希均已冻结。
- 12 次主矩阵全部成功退出，所有最终 SRT 时间轴有效；启用重切分的运行不再出现文本丢失。
- 三个 `quality` 运行合计只执行 1 个 retry 窗口，接受窗口为 0，低于最少 3 个的证据门槛。
- 热词隔离 B/D 运行没有改善冻结的目标专名命中，也没有产生可审核的 accepted window。
- 代表样本的旧 CLI 输出与 `speed` 输出逐字节一致。

## 本轮决策摘要

- Attempt 3 判定为证据不足，不能保留 `quality=apply` 作为默认发布候选。
- 按预先固定的条件通过路线，将 `quality` preset 默认改为 `dry_run`。
- 显式 `--asr-retry-mode apply` 继续保留，不改变固定 recipe、预算、硬拒绝和事务校验。

## 实际执行的操作

- 修改 `src/core/asr_runtime.py`，将 `quality` preset 的 retry 默认值改为 `dry_run`。
- 同步修改真实媒体验收 runner、相关测试、README 和 Web 帮助文案。

## 验证结果

- 源码修改后的完整回归与发布候选矩阵尚待执行。

## 未解决问题与下一步

- 完成自动化回归并创建新的 remediation commit。
- 重新冻结 evaluated SHA，从三段 12 次主矩阵起点完整重跑最终发布候选配置。
- 生成匿名报告时保留 Attempt 2 结构失败和 Attempt 3 apply 证据不足两次结论。
