# 轮次记录：实施可靠性与影院字幕工作站升级

- 时间：2026-07-20 19:45:58 +08:00
- 用户目标：完整实施已确认的模型预检、受控下载、漏识别诊断、信息架构和视觉升级计划。

## 已知事实与证据

- 已在本轮开始前递归读取 `agents/thinking/` 全部文件。
- 已确认当前 Web 会把缺失模型交给 faster-whisper 联网查找，导致任务进入转写后才失败。
- 当前前端为 `web/index.html` 单文件，无 CDN、无 npm 构建；测试基线此前通过。

## 决策摘要

- 先实现共享模型定位、Web 预检和受控下载，再实现 ASR 复核报告，最后调整 UI。
- Web 任务只能加载已验证的本地模型；下载必须二次确认且使用固定模型/来源白名单。
- 漏识别诊断只产生 warning 报告，不重跑、不换模型、不替换输出。
- UI 保持本地字幕工作站方向，视觉签名只放在真实五阶段时间轴。

## 执行操作

- 已读取 `superdesign` 与 `frontend-design` 技能。
- 已验证 Superdesign CLI 0.4.0，并读取最新 INIT/SUPERDESIGN 指南。
- 已建立被 Git 忽略的 `.superdesign/` 项目设计上下文。
- 新增共享 ASR 模型定位器，支持项目模型目录、Hugging Face snapshot 和用户绝对路径，并校验 `config.json` 与非空 `model.bin`。
- 新增模型计划、单例受控下载及状态 API；Web 单任务、批量和失败重试均在改变任务状态前做最终模型预检，并强制本地加载。
- 将内置、示例及 Web/Pipeline 默认模型统一为 `small`，不迁移已有本地配置。
- 新增只读 VAD 覆盖诊断和 `asr_review.json`，把摘要与下载入口接入单任务及 Pipeline。
- 重组批量配置、单任务状态、运行环境和日志交互；移除前端小样本上传，后端兼容入口保留。
- 统一深色 token、内联 SVG 导航、真实五阶段状态、焦点与减弱动画支持。
- 新增模型定位、下载、HTTP 409 零任务变更和 VAD 区间测试。

## 验证结果

- 完整 `pytest` 通过：417 项测试。
- 导入检查、`subtitle_translate.py --self-test`、`quality_checker.py --self-test` 均通过。
- `start_web.ps1 -Smoke -NoBrowser -NonInteractive` 通过，未启动模型下载或媒体处理。
- 实际启动 Web 后 `/` 与 `/api/runtime/diagnostics` 均返回 200。
- `desktop/main.js`、`desktop/preload.js`、`desktop/launch.js` 和 `web/index.html` 内联脚本语法检查通过。
- `git diff --check` 通过，仅有 Git 的 LF/CRLF 提示。
- 使用 Playwright 在 1920、1280、900、700px 检查，无水平溢出；单任务页和模型确认弹窗完成视觉检查。

## 未解决问题与下一步

- 未执行真实大模型网络下载，自动化测试均使用 mock，避免产生大文件和外部网络副作用。
- 未使用真实电影素材做 ASR 人工复现；代码、合成区间与接口契约已覆盖。
- 正式 Release 仍保持 0.6.2；本轮未改变 Electron 启动、资源目录、数据目录或发布文件名。
