# 轮次记录：批量长片可靠性实现

- 时间：2026-07-22
- 用户目标：在 `codex/batch-longform-reliability` 实现完整批量长片可靠性契约，验证后提交；随后同步最新 `origin/main` 并重跑回归。

## 已知事实与证据

- 本轮开始前已递归读取 `agents/thinking/` 全部文件，工作区初始干净，分支为 `codex/batch-longform-reliability`，基线为 `07c78e4`。
- 原实现按 stem 命名状态、音频和输出，scan 与 retry planner 会写状态，Web 仅有进程内锁并暴露绝对路径和原始错误。
- 用户后续通知 ASR PR #6 已合入，`origin/main=ac7db02529a05f5d13b115c37b18f4846e49496a`；本轮停止时尚未执行该合并。

## 决策摘要

- 以独立可靠性模块统一 task identity、只读计划、阶段/产物指纹、Provider 本地预检、run record、Windows PID FILETIME 和跨进程锁。
- task ID 使用 NFC、统一分隔符和 Windows casefold 后的相对路径哈希；持久化/API 相对路径保留 NFC 原始大小写。
- retry 计划保持纯读取，执行者显式 apply；`input` 到 `final_output` 均使用阶段签名，final output 使用产物集合指纹。
- Windows worker 锁采用同一锁文件的启动门与 worker lease 双字节显式交接，不依赖继承 HANDLE 自动转移锁所有权。

## 实际执行的操作

- 新增 `pipeline_reliability.py`，实现只读 `PipelinePlan`、无碰撞身份、输入/产物指纹、阶段签名、归属冲突、原子 run record、跨进程锁和 PID FILETIME。
- 扩展任务状态、worker、阶段执行、事件日志和 Web API，加入迁移、归档元数据、run_id、纯 retry、脱敏 progress/log 和本地 Provider 预检。
- 新增长片专项测试并更新受新契约影响的旧测试；测试覆盖同 stem、历史归属、迁移验证、final output、真实进程锁、worker lease、PID 复用、服务重启/stale 和敏感信息清洗。

## 验证结果

- 最新 focused tests：`39 passed`（长片可靠性、artifact、离线 E2E、task state 等组合）。
- 此前修改阶段完整 pytest 曾全部通过；最新一次通过 `subst` 执行的 full tests 有 3 项失败，其中 2 项为便携构建测试的 Windows 长路径，1 项为 `subst` 导致的 VideOCR 盘符解析差异。
- 合并前完整 pytest、导入检查、两个 self-test、Electron Node 检查和 `git diff --check` 曾通过；最新安全/锁改动后尚未重新执行整套命令。

## 未解决问题与下一步

- 尚未 fetch/merge `origin/main@ac7db02`，也未执行合并后的完整回归。
- 便携构建测试在当前较长 worktree 路径下仍受 Windows `MAX_PATH` 影响；已撤销对全局 `tests/conftest.py` 临时目录长度的修改，未用全局夹具掩盖问题。
- 当前实现应先保存提交；后续继续时先读取本记录，再合并最新 main、解决冲突并重跑完整回归。
