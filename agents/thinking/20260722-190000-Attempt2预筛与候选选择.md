# 轮次记录：Attempt 2 预筛与候选选择

- 时间：2026-07-22
- 用户目标：在 remediation 后重新冻结环境，完整重跑六视频预筛，并生成三个匿名正式候选供用户确认。

## 已知事实与证据

- remediation commit 为 `5aef1263d5d40e219d64276cc8faf3630fbfcbb9`，Attempt 2 的 `evaluated_sha` 与 `acceptance_runner_sha` 均为该 SHA。
- VideOCR GPU、`small` 和 `large-v3` 都在 Attempt 2 冻结前重新通过真实预检。
- 六个来源各三个 60 秒窗口的 WAV、音量、`small/speed` 和 VideOCR GPU 已全部重跑成功。
- 两个画面字幕语言不稳定的来源经 `small/auto` 检测均为英语主音轨，不适合进入固定法语矩阵。
- 另一个法语来源总时长小于 180 秒，不满足正式片段长度契约。

## 决策摘要

- 候选 `sample-01` 选取低音量法语双语字幕来源，用于压力测试 suspicious cue 和 retry。
- 候选 `sample-02` 选取清晰近场法语访谈来源，作为正常对话控制场景。
- 候选 `sample-03` 选取较低音量的法语舞台演讲来源，用于检查远场语音、长句重切分和时间轴。
- 不把英语来源强制当作法语样本，不为凑够人数放宽片段时长契约。

## 执行操作

- 生成 Attempt 2 私有环境指纹，保留 Attempt 1 失败证据。
- 生成 18 个匿名 WAV 窗口，采集音量并运行 18 个独立 `small/speed` 进程。
- 使用固定 ROI、5 秒级采样和独立 ASCII runtime 运行 18 个 VideOCR GPU 窗口。
- 对两个非法语候选各补充一个 `small/auto` 语言检测窗口。

## 验证结果

- `sample-01` 预筛平均音量约 -26.7 dB，三窗共 18 个 suspicious cue，具有足够 retry 压力。
- `sample-02` 预筛平均音量约 -17.7 dB，三窗共 1 个 suspicious cue，可作为清晰对照。
- `sample-03` 预筛平均音量约 -24.1 dB，三窗无 suspicious cue，OCR 覆盖稳定，适合验证不应过度 retry 的场景。
- 三个候选都有稳定的法语硬字幕弱证据，且可提取 180–300 秒连续片段。

## 未解决问题与下一步

- 等待用户确认 `sample-01..03` 三个匿名候选。
- 确认后固定片段哈希、ROI、法语 fixed 模式和 Profile，然后开始 12 次主矩阵。
