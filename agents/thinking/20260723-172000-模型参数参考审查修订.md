# 轮次记录：模型参数参考审查修订

- 时间：2026-07-23 17:20:00 +08:00
- 用户目标：根据 PR #11 的逐段审查意见，在合并前修正文档中的事实映射错误并收紧若干表述。

## 已知事实与证据

- 本轮开始前已递归读取 `agents/thinking/` 全部文件。
- 用户附件指出 4 个必须修复的问题：首轮 `initial_prompt` 与 glossary 注入边界、DeepSeek `max_tokens` 使用路径、Flash/Pro 模型选择与 thinking-mode 控制混淆、Whisper decode threshold 与 segment 输出指标混淆。
- 源码复核确认：首轮 ASR `initial_prompt` 只由 `asr_hotword_prompt` 构建；局部 retry 才按窗口和邻近 cue 匹配 glossary `source`、`aliases`、`asr_variants`。
- 源码复核确认：Anthropic 请求发送 `max_tokens: 4096`；OpenAI-compatible 翻译请求不发送 `max_tokens`；Provider 连接测试发送 `max_tokens: 256`。
- 源码复核确认：Provider 模板配置 Flash/Pro 模型 ID，但未暴露或发送 DeepSeek thinking-mode 控制。
- 源码复核确认：项目未显式传入 `compression_ratio_threshold`、`log_prob_threshold`、`no_speech_threshold`；项目记录 `segment.compression_ratio`、`segment.avg_logprob`、`segment.no_speech_prob` 并使用独立 review 阈值。

## 决策摘要

- 逐项修正 4 个 P1 映射错误。
- 将 Provider 描述改为 endpoint metadata 与翻译模型字段，不再简化为只保存 API Base、API Key 和模型。
- 将配置优先级拆成 ASR quality-loop、Translation endpoint、Translation style/terminology 三条链。
- 将实验规则证据拆成 ASR 已验证契约与翻译 proposed/experimental 契约。
- 增加项目证据 SHA 和本地安装的 faster-whisper/CTranslate2 版本。

## 执行操作

- 修改 `docs/model_tuning_reference.md`。
- 新增本 thinking 记录。
- 未修改运行时行为、模型参数、默认 preset、API、UI、版本号、Tag 或 Release。

## 验证结果

- `git diff --check` 通过。
- `git status --short --branch --untracked-files=all` 显示本轮只修改 `docs/model_tuning_reference.md` 并新增本 thinking 记录。
- 旧错误表述扫描通过，未再命中首轮 prompt/glossary、OpenAI-compatible `max_tokens=4096`、Flash/Pro 等同 thinking-mode、threshold/segment 指标混淆等旧描述。
- UTF-8 检查通过，无 replacement character。
- 双向 Unicode 控制字符扫描通过。
- 相对 Markdown 链接检查通过。
- 文档表格行基础扫描通过。
- 未发现绝对本地路径、API Key、私有媒体名称、完整 transcript 或虚构 benchmark。

## 未解决问题与下一步

- 提交并推送到 `codex/model-tuning-reference`，更新 PR #11。
