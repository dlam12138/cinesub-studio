# 轮次记录：ASR 验收契约审查修复

- 时间：2026-07-22 17:00:39 +08:00
- 用户目标：修复单次验收 CLI 的 `--run-id` 兼容性，并加强每条 campaign report 相对计划契约的配置验证；保持 28-run 设计不变。

## 已知事实与证据

- 上一提交把 `run --run-id` 设为必填，破坏了原有单次运行调用兼容性。
- `validate_campaign_reports()` 仅检查任务身份和 `local_files_only`，没有逐条核对 ASR 模式、语言及 profile 配置。
- 工作区开始时干净，当前分支头为 `c46954a`。

## 本轮决策摘要

- `--run-id` 恢复为可选；省略时由 scenario（缺省为 `sample_id-primary`）和 profile 生成安全文件名，显式 run ID 保持原值。
- `run_profile()` 统一使用规范化后的 ID 写日志、错误、JSON 报告和返回值。
- campaign validator 在 pairwise quality/control 检查前，逐条验证 `asr_mode`、`language`、模型、preset、词级时间戳、重切分和 retry mode。

## 实际执行的操作

- 修改 `src/tools/real_media_acceptance.py` 的单次 run 参数、ID 规范化和逐报告计划校验。
- 增加 CLI 省略 run ID、文件与报告使用派生 ID，以及七类计划字段漂移拒绝测试。
- 未修改 campaign 样本、场景、profile 或 28-run 数量。

## 验证结果

- 定向测试共 34 项通过。
- 排除两个已知 Windows 长路径 portable builder 用例后，全量可行回归通过。
- `git diff --check` 通过。

## 未解决问题与下一步

- 两个 portable builder 测试仍受当前 worktree 长路径的 `WinError 206` 限制，与本轮改动无关。
- 私有六样本 28-run 真实矩阵仍待媒体就绪后执行。
