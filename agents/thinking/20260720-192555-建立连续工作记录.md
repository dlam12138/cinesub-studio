# 轮次记录：建立连续工作记录

- 时间：2026-07-20 19:25:55 +08:00
- 用户目标：在项目中新增 `agents` 文件夹，持续记录每轮的决策与工作，并要求每轮开始前读取 `thinking` 中的全部文件。

## 已知事实与证据

- 仓库根目录已有 `AGENTS.md`，此前没有跨轮工作记录约束。
- 用户要求使用目录名 `agents`，与现有禁止提交的 `.agents/` 不是同一路径。
- 上一轮只读分析了外部失败报告、截图、流水线状态和字幕覆盖，没有修改项目源码。
- 已确认一个批量任务选择了本地不存在的模型，并在离线查找模型快照时失败；字幕对比也显示存在若干无相邻识别结果的原字幕条目。

## 决策摘要

- 使用 `agents/thinking/` 保存可审计的决策与执行摘要。
- 将强制读取和追加记录的规则写入根 `AGENTS.md`，使后续 agent 也能遵守。
- 不记录隐藏思维链；以目标、事实、决策、操作、验证和下一步代替。

## 执行操作

- 更新根 `AGENTS.md`，加入“Agent 连续工作记录”规则。
- 新增 `agents/README.md`。
- 新增 `agents/thinking/README.md` 和本轮记录。

## 验证结果

- 已递归读取 `agents/thinking/` 中的全部文件，内容和 UTF-8 中文显示正常。
- `git diff --check` 通过，没有空白字符错误。
- Git 状态只显示本轮预期的 `AGENTS.md` 修改和 `agents/` 新目录。

## 未解决问题与下一步

- 后续每轮开始前递归读取本目录全部文件。
- 后续每轮结束前新增独立记录，不覆盖历史记录。
- 若继续修复失败报告中的问题，应先定位所选模型预检、离线模型加载和对应测试。
