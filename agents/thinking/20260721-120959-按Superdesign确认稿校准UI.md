# 轮次记录：按 Superdesign 确认稿校准 UI

- 时间：2026-07-21 12:09:59 +08:00
- 用户目标：纠正产品实现与已确认 Superdesign 画布差异过大的问题，要求界面按确认稿实际呈现，而不是只借鉴设计语言。

## 已知事实与证据

- 本轮开始前已递归读取 `agents/thinking/` 全部文件，并重新读取 Superdesign、frontend-design 和 Playwright 技能。
- 重新下载确认稿 `c75efb2e-6fc8-44af-ba2f-29db11384e76` 后确认：画布没有此前实现增加的 Workspace 大标题层。
- 截图对照显示主要偏差为侧栏比例、批量页顶部位置、任务桌面纵向结构、右侧栏宽度和检查卡高度。
- 确认稿的核心桌面几何为：256px 左栏、24px 主区边距、99px 五阶段横条、24px 栏间距和 360px 右侧配置栏。

## 决策摘要

- 将确认稿作为视觉单一事实来源，撤销此前自行增加的五页 Workspace 标题体系。
- 保留真实运行检查及其跳转功能，但将操作按钮压缩为确认稿中的状态图标形式。
- 动态无任务状态不再先显示无意义的 0 统计，直接呈现确认稿的空队列工作台；存在任务时仍保留完整进度、筛选和任务表格。
- 保留真实版本号、本地品牌资源、三个 ASR 模式和全部前端契约，不复制确认稿里的错误占位版本或外部依赖。

## 实际执行的操作

- 重新获取并渲染 Superdesign 确认稿，在 1280×720 下与实际页面逐区对照。
- 将应用壳、品牌卡、导航激活态、主区宽度、五阶段横条、任务桌面、检查卡、空状态、支持折叠区和配置栏按画布尺寸重构。
- 删除五个页面中画布不存在的 Workspace 标题层；运行环境刷新操作移入摘要卡标题区。
- 将批量阶段标记改为确认稿的 `STAGE 01` 至 `STAGE 05`，并补齐确认稿中的播放、文件和复核视觉符号。
- 更新前端契约测试，检查批量页直接以阶段横条开场、360px 配置栏、Batch Queue 和空队列结构。
- 生成新的实际页面截图 `output/playwright/superdesign-faithful-batch-1280.png`。

## 验证结果

- 1280×720 实际截图与确认稿的主坐标、栏宽、阶段条、任务卡、检查区、空状态和配置栏已基本重合。
- 390px 下五个页面均无页面级水平溢出。
- 浏览器控制台 0 错误、0 警告。
- 完整 `pytest -q` 通过；前端专项测试 13 项通过；内联 JavaScript 语法检查通过。
- `git diff --check` 和外部依赖审计通过，未引入 Tailwind CDN、Iconify 或 Google Fonts。
- 本轮 QA 服务器、Superdesign 预览服务器和浏览器会话均已关闭，7860 无监听进程。

## 未解决问题与下一步

- Superdesign 确认稿只直接生成了批量页，其他四页按同一设计系统和“无额外 Workspace 标题层”规则校准，未虚构不存在的独立画布。
- 动态运行数据会使检查卡文字与确认稿静态示例不同，这是保留真实功能和状态的预期差异。
- 未下载模型、处理真实媒体或构建正式 Release；Electron 0.6.2 契约保持不变。
