# 轮次记录：批量长片可靠性审查收口

- 时间：2026-07-22
- 用户目标：完成批量长片可靠性分支，同步 ASR PR 合入后的最新 `main`，执行完整回归并创建 PR。

## 已知事实与证据

- 主体实现提交为 `090796d`，共同初始基线为 `07c78e4`；ASR PR #6 合入后的 `origin/main` 为 `ac7db02`。
- 专项测试覆盖纯计划、身份碰撞、旧状态迁移、阶段失效、产物归属、归档、真实跨进程锁、worker lease、PID FILETIME、stale、服务重启和 API/日志脱敏。
- 长路径 worktree 中完整 pytest 只有两个 portable builder 测试因 Windows `MAX_PATH` 失败；同一测试集需要在短路径 detached worktree 复核。

## 本轮决策与操作

- 停止未及时收口的子代理，保留其已提交实现并由主代理审查。
- 将任务哈希使用的 casefold 相对路径与持久化/API 展示的 NFC 原始大小写相对路径分离。
- API 不再从 state 文件名或媒体 basename 推导缺失的 `task_id`；未迁移旧状态只展示为空身份且不能据此下载产物。
- progress 移除语言检测 block 明细，避免潜在 transcript 暴露；新增回归测试。
- scan 计划显式标记可迁移的 legacy ASR 签名；输入指纹整体进入 audio 阶段签名。

## 验证结果

- 长片专项组合：47 passed。
- `git diff --check`：通过。
- 长路径完整 pytest：除两个已知 `MAX_PATH` portable builder 测试外其余通过；待短路径复核全部测试。

## 未解决问题与下一步

- 提交本轮修正，合并 `origin/main@ac7db02` 后在短路径 detached worktree 执行完整 pytest、导入、自检、Web/HTTP、Node 和 diff 检查。
- 验证通过后推送分支、创建 PR、等待 CI；不构建 ZIP、不升版、不打 Tag。
