# 轮次记录：Stage 3 质量 Campaign 工具与素材门禁

- 时间：2026-07-28
- 用户目标：完成 Stage 3 ASR 与翻译质量 Campaign，包括评测工具、28-run 私有实测、盲评、匿名证据、CI 和最终门禁。

## 已知事实与证据

- 基线 `main` 与 `origin/main` 均为 `403f21818cd89f7537077b6f85da4fb55c28cae8`，Stage 2 为 Pass。
- Stage 3 分支为 `codex/stage3-asr-translation-quality-campaign`。
- 工具提交为 `b379e0beee7cda1a5fc8d0e549384006a23f6f23`，同一 SHA 已写入 ignored 的 28-run 合同。
- 本地仅有 3 个旧法语正式片段；另有法语长片证据，但没有合同要求的英语、普通话和真实双语切换私有样本。
- 没有包含 6 个样本、逐条 `authorized=true`、reference 类型和 reference 路径的私有 manifest。
- `large-v3` 在仓库模型目录存在；旧冻结证据曾验证 `small`，但本轮尚未取得可运行的 6 样本 manifest，因此未开始模型加载或 ASR。

## 本轮决策摘要

- 新增独立 ASR evaluator，JiWER 只用于 `gold_verbatim`，发行字幕和 OCR 不进入正式 WER/CER。
- 扩展翻译 benchmark，结构门禁、source hash、SacreBLEU 指标和答案键均与 reviewer 盲评文件分离。
- retry 审计只读取现有 hashes/metrics；生产报告不保存候选正文，因此不得伪造 retry 文本盲评或据此晋级 apply。
- 不复用旧 run，不用 3 个法语样本冒充 6 样本，不把 OCR 或机器翻译输出提升为金标。

## 实际执行的操作

- 创建 Stage 3 分支，加入 JiWER/SacreBLEU 开发依赖。
- 新增 `src/tools/asr_quality_evaluation.py`，扩展 `real_media_acceptance.py` 与 `translation_quality_benchmark.py`。
- 新增和扩展永久测试，创建工具提交。
- 生成 ignored 的 28-run 合同与 ASR/翻译私有 manifest 模板。
- 只做匿名化素材数量、语言标签和 reference 可用性审计；未回显或提交私有路径、文件名、字幕正文或完整媒体哈希。

## 验证结果

- 工具定向测试 34 项通过。
- 修改完成前的全量 pytest、导入检查、两个 self-test、Web smoke、Electron JavaScript 检查和 `git diff --check` 通过；最终小型路径修正后再次通过全部定向测试。
- 新增评测文件 Ruff 为 0；仓库 Ruff 相对父基线未增加。
- 28-run 合同包含 28 个唯一 run，evaluated SHA 为工具提交 SHA。

## 未解决问题与下一步

- 需要用户提供或确认 ignored 私有 manifest 中 6 个授权样本，至少覆盖既定法语、英语、普通话和真实双语场景，并提供合规 reference。
- 取得 manifest 后先验证本地 `small`/`large-v3` 均可 CUDA/float16、`local_files_only=True` 加载，再执行全新 28-run。
- ASR 完成后冻结 source SRT，再运行翻译 T1；没有可靠双语 reference 时 fidelity 必须保持 `inconclusive`。
- 在完整证据前不得改生产默认、创建 Tag、Release 或 Release Prep。
