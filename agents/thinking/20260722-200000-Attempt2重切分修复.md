# 轮次记录：Attempt 2 重切分修复

- 时间：2026-07-22
- 用户目标：确认三个匿名样本后执行 v0.7.1 真实媒体正式矩阵，并按固定规则处理验收缺陷。

## 已知事实与证据

- 三个冻结样本的正式 VideOCR 弱证据均成功生成。
- Attempt 2 的 12 次主矩阵全部正常退出，但所有启用重切分的运行均报告 `text_conservation_failed` 并回退原 artifact。
- 真实 faster-whisper 法语词序列含少量携带文本但时间长度为零的缩写片段；旧实现过滤这些片段，触发文本守恒保护。

## 本轮决策摘要

- 按验收契约终止 Attempt 2，不继续热词 A/B 或形成通过结论。
- 保留 Attempt 2 的匿名失败结论，修复后使用新的 evaluated SHA 从正式矩阵起点完整重跑。
- 保留所有非空词文本及原顺序，只使用有效时间词计算重切分 cue 的边界。

## 实际执行的操作

- 修改 `src/core/subtitle_resegment.py`，不再删除零时长文本片段，不再按时间重新排序词序列，并增加有限时间检查与分组时间边界计算。
- 修改 `tests/test_asr_quality_loop.py`，增加法语缩写形态的零时长文本片段守恒测试。
- 使用冻结的真实样本做一次私有 remediation probe。

## 验证结果

- 定向 ASR quality loop 测试通过。
- 真实 remediation probe 从 48 条原始 cue 生成 57 条重切分 cue，739 个词片段守恒，`fallback_reason` 为空。

## 未解决问题与下一步

- 尚需完成全部自动化回归、创建并推送 remediation commit。
- 随后重新执行模型与环境冻结，并从三个样本的 12 次主矩阵起点完整重跑。
- Attempt 2 的结构失败结论必须保留在最终匿名报告中。
