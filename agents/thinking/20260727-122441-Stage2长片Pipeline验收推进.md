# 轮次记录：Stage 2 长片 Pipeline 验收推进

- 时间：2026-07-27 12:24:41 +08:00
- 用户目标：按计划继续 v0.7.1 Stage 2 长片 Pipeline 验收，先合并三个冻结前修复 PR，再准备并执行正式验收分支。

## 已知事实与证据

- 本轮开始前已递归读取 `agents/thinking/` 全部文件。
- 起点为 `codex/pipeline-privacy-redact` 本地提交 `0c83b48`，`main` 当时为 `22cee9c`。
- 三个冻结前 PR 均已创建、CI 通过并 squash 合并到 `main`：
  - PR #13：Web path/log redaction。
  - PR #14：Pipeline signature completeness。
  - PR #15：Public plan fingerprint/path redaction。
- 最终验收分支从 `main@252f75c7bea15070fd8a644694c0dd74ce3ade66` 创建。
- `large-v3` 已从 `hf-mirror` 下载到项目 `models/`，项目 locator 返回 available，CTranslate2 `contains_model=True`。
- 运行态已快照到 Git 忽略的 pipeline-private 目录，后续本地输入、输出、日志、模型和私有证据均未纳入 Git。

## 决策摘要

- PR#2 的 translation profile hash 只纳入翻译实际相关字段；由于 `max_cps_zh` 和 `max_chars_per_subtitle_zh` 会进入 `translate_srt`，仍保留在 translation 签名中，避免旧译文错误复用。
- PR#3 将公开 plan/scan 响应改为短 hash 前缀，内部 worker 仍使用完整 fingerprint。
- Stage 2 公开结论写为 Conditional Pass：S1-S3 用真实私有媒体重新跑，S4-S12 主要由同一冻结基线上的永久自动化可靠性测试覆盖。

## 实际执行的操作

- 推送、创建并合并 PR #13、#14、#15。
- 执行统一门闸：全量 pytest、导入 smoke、两个 self-test、Web smoke、Electron JS 语法检查和 `git diff --check`。
- 创建 `codex/pipeline-longform-acceptance`。
- 快照旧 `output/`、`logs/`、`work/translation-cache`、`archive/`、`failed/`，并重建空目录。
- 下载并验证 `Systran/faster-whisper-large-v3` 平铺模型文件。
- 使用私有样本执行：
  - S1：large-v3 CUDA/float16 cold run，`local_files_only=True`，no-translate，完成。
  - S2：相同配置复跑，skip completed。
  - S3：单 run 双 same-stem 输入，两个 task/output stem 隔离。
- 执行 DeepSeek 短调用 smoke，仅记录结构化状态，不记录响应正文或密钥。
- 执行专项测试：`test_pipeline_longform_reliability.py`、`test_pipeline_preflight_consistency.py`、`test_pipeline_offline_e2e.py` 和 `test_pipeline_privacy_canary.py`。
- 新增公开匿名交付物和 `.gitignore` allowlist：
  - `acceptance/v0_7_1_pipeline_longform_acceptance.md`
  - `acceptance/reports/v0_7_1_pipeline_summary.json`
  - `acceptance/reports/v0_7_1_pipeline_scenarios.csv`

## 验证结果

- PR #13/#14/#15 GitHub checks 均通过并已合并。
- 全量 pytest 通过。
- 导入 smoke 通过。
- `subtitle_translate.py --self-test` 和 `quality_checker.py --self-test` 通过。
- `start_web.ps1 -Smoke -NoBrowser -NonInteractive` 通过。
- `/` 与 `/api/runtime/diagnostics` HTTP 状态均为 200。
- `node --check desktop/main.js`、`desktop/preload.js`、`desktop/launch.js` 通过。
- `git diff --check` 通过。
- 交付物 JSON/CSV 解析通过；隐私扫描未命中绝对本地路径、真实密钥形态、私有目录名或输入/输出目录路径。

## 未解决问题与下一步

- 公开 Stage 2 结论为 Conditional Pass，因为本轮没有对 S4-S12 全部做真实私有媒体端到端重跑；这些场景依赖永久自动化可靠性测试和专项 smoke 作为证据。
- 后续如需升为无条件 Pass，应在私有目录中对 12 个场景全部进行真实媒体端到端执行，特别是包含真实翻译和完成后归档的 S12。
- 需要提交并推送 `codex/pipeline-longform-acceptance`，创建验收 PR。
