# 轮次记录：v0.7.1 源码候选合并

- 时间：2026-07-22
- 用户目标：从干净的 `origin/main` 合并 v0.7.1 真实媒体验收分支，修正文档契约并在不升版、不打包的前提下建立双线优化基线。

## 已知事实与证据

- 合并前工作区干净，`origin/main` 为 `ff2f48b`，验收分支头为 `2bdea22`。
- 正式运行时代码验收 SHA 为 `dd170e3`，`2bdea22` 仅提交匿名化验收记录。
- `VERSION`、Python 和 Electron 版本仍为 `0.6.2`。

## 本轮决策摘要

- 使用非快进合并保留验收提交历史。
- README 明确 `main` 源码候选与 0.6.2 正式便携包的差异。
- 配置优先级补充 `quality_preset`，验收记录明确报告提交 SHA。

## 实际执行的操作

- 从 `origin/main` 创建 `codex/v0.7.1-source-merge` 并合并远端验收分支。
- 修改 `README.md`、`AGENTS.md` 和公开验收 Markdown。

## 验证结果

- 全量 pytest、导入检查、两个 self-test、Web smoke、两个 HTTP 200、Electron JavaScript 语法检查和 `git diff --check` 均通过。
- 未修改版本元数据，未构建便携包，未处理真实媒体或下载模型。

## 未解决问题与下一步

- 推送并合并源码 PR，记录新的共同 `main` SHA。
- 从共同 SHA 建立批量长片可靠性与 ASR 基线稳定性两个独立 worktree。
